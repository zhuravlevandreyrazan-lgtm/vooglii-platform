import { WorkspacePlaceholder } from "@/widgets/workspace-placeholder";

export default function SystemPage() {
  return (
    <WorkspacePlaceholder
      description="System workspace will surface source health, sync state, diagnostics, and confidence infrastructure."
      eyebrow="System Workspace"
      title="System"
    />
  );
}
